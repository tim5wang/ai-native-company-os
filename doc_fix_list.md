# ANC 产品计划修复清单

> 基于 proposal 评审（2026-06-02）整理的核心问题和风险，逐一记录状态、影响范围和解决方案。

---

## 如何用这个清单

- **状态**：🟢 未开始 / 🟡 进行中 / 🔴 阻塞 / ✅ 已完成
- **优先级**：P0（必须解决才能继续）/ P1（重要但不阻塞）/ P2（长期优化）
- 每个问题尽量关联 proposal 原文中的章节

---

## 1. ✅ 工程范围被严重低估

| 字段 | 内容 |
|---|---|
| **优先级** | P0 |
| **关联章节** | 第 6.1 节、第 6.2 节、第 9 节、第 10 节 |
| **问题描述** | MVP 的 "必须做" 清单包含 8 个主要系统（Builder Agent、Business IR、App Generator、Workflow Engine、Permission System、Audit & Logging、Version Management、AI Resource Governance），即使全部简化实现，也是 6-12 个月的工作量。 |
| **影响** | 范围过大 → 周期过长 → 看不到阶段性成果 → 团队士气 + 投资人信心下降 |
| **解决方案** | 在 MVP 内进一步收窄优先级。建议区分 Phase 1a / Phase 1b：<br>1. Phase 1a 只做 Builder Agent + 最小 IR + App Generator + 基础运行，不做权限系统、版本管理、AI Resource Governance。<br>2. 明确每个系统在 Phase 1 的最小可行形态是什么。<br>3. 对每个系统评估 "不做会怎样"，砍掉不影响闭环的。 |
| **修改内容** | 第 6.1 节拆分为 Phase 1a（6 项）+ Phase 1b（4 项）；第 6.2 节新增 Phase 1a 暂不做列表；第 9 节 P0/P1 重新对齐；第 10 节 Phase 1 拆分为 Phase 1a/1b |
| **负责人** | |
| **截止日期** | |
| **状态** | ✅ 已完成 |

---

## 2. ✅ Business IR 过度设计风险

| 字段 | 内容 |
|---|---|
| **优先级** | P0 |
| **关联章节** | 第 7.2.2 节 |
| **问题描述** | Business IR 包含 15+ 个部分（Entities、Fields、Relations、Pages、Forms、Workflows、Roles、Permissions、Notifications、AuditRules、Tests、ModuleDependencies、AIResourceDependencies、RuntimePolicies、Versions、ModuleLock、AIResourceLock）。问题：<br>1. IR 的 completeness 问题——schema 要覆盖到什么程度才能生成一个真实可用的应用？<br>2. 演进负担——IR 的每次变更都需要 Builder Agent、App Generator、Workflow Engine、Runtime 协调修改。<br>3. 如果一开始把 IR 设计得太大，会延迟端到端验证的时间。 |
| **影响** | 管线无法闭环 → 前期投入大量时间在抽象上，用户看不到产物 |
| **解决方案** | Phase 1 做一个严格子集——建议只包含：`Entities`、`Fields`、`Relations`、`Workflows`（含 StatusTransitions）、`Roles`、`Permissions`。<br>其他部分（AuditRules、Notifications、ModuleDependencies、AIResourceDependencies 等）放到 Phase 2 再加入。<br>先跑通一个 IR → 生成器 → 运行应用的完整管线，再逐步扩展。 |
| **修改内容** | 第 7.2.2 节将 IR 树拆分为 "Phase 1a 最小核心 IR"（10 项）和 "Phase 1b/Phase 2 扩展 IR"（7 项） |
| **负责人** | |
| **截止日期** | |
| **状态** | ✅ 已完成 |

---

## 3. ✅ Agent 拆分过细，架构复杂

| 字段 | 内容 |
|---|---|
| **优先级** | P0 |
| **关联章节** | 第 8 节 |
| **问题描述** | 提案定义了 6 个 Agent（Builder、Schema、Workflow、UI、Test、Ops），存在三个问题：<br>1. **编排复杂性**——谁协调这么多 Agent？如果 Builder Agent 来做，它就变成 meta-agent，LLM 调用链更长。<br>2. **上下文碎片化**——Schema Agent 需要知道 Workflow 的决策，UI Agent 需要 Schema 上下文，Agent 之间频繁通信。<br>3. **成本问题**——每个 Agent 调用都是 LLM token，6 个 Agent 意味着多轮对话。 |
| **影响** | 增加 LLM token 消耗 + 延迟 + 调试难度 + 系统脆弱性 |
| **解决方案** | 简化架构：<br>1. **Builder Agent**（唯一面向用户的 Agent）——负责对话、规划、需求澄清。<br>2. Schema/Workflow/UI/Test 改为 **工具函数（Tools/Functions）** 而非独立 Agent——Builder Agent 根据规划直接调用它们生成 IR 片段，不需要独立对话。<br>3. **Ops Agent** 保持独立，但只用于运维诊断，Phase 2 之后才启用。<br>这样从 6 个 Agent 减为 2 个（Builder + Ops），其余为工具函数。 |
| **修改内容** | 第 8 节重构：移除 Schema/Workflow/UI/Test 独立 Agent，改为 Builder Agent 的工具函数（8.2.1-8.2.4）；Ops Agent 标记为 Phase 2+ 启用；新增设计原则说明 |
| **负责人** | |
| **截止日期** | |
| **状态** | ✅ 已完成 |

---

## 4. ✅ AI Resource Registry 对 MVP 太重

| 字段 | 内容 |
|---|---|
| **优先级** | P1 |
| **关联章节** | 第 7.2.4 节 |
| **问题描述** | 提案中的 AI Resource Registry 包含：Prompt 版本管理、模型配置、知识库、工具、连接器、成本统计、质量指标——几乎是一个完整的 LLMOps 平台。在 MVP 阶段构建这个系统会严重分散精力。 |
| **影响** | 分散核心管线验证的精力；AI 治理虽然重要，但 MVP 阶段客户还不关心这个 |
| **解决方案** | Phase 1 只做最轻量的记录：每次生成时把使用的 Prompt 模板 ID、模型名称、知识库 ID 写到一个 `generation-manifest.json` 中。不做 Registry UI、不做版本管理、不做成本统计。完整的 AI Resource Registry 放到 Phase 3。 |
| **修改内容** | 第 7.2.4 节添加 Phase 1a/1b/3 分阶段说明；Phase 1a 仅用 generation-manifest.json；第 7.1 架构图移除 AI Resource Registry 独立层；第 7.2.1 Builder Agent 输入改引用 generation-manifest.json |
| **负责人** | |
| **截止日期** | |
| **状态** | ✅ 已完成 |

---

## 5. ✅ Module Registry UI 对 Phase 1 太重

| 字段 | 内容 |
|---|---|
| **优先级** | P1 |
| **关联章节** | 第 7.2.3 节 |
| **问题描述** | 提案中的 Module Registry 包含：元数据管理、标签分类、SemVer + 变更日志、使用统计、访问控制（公开/私有/付费）、升级检测、迁移指南。在概念验证阶段构建一个 Web Registry 是过早优化。 |
| **影响** | 投入大量前端 + 后端资源在 Registry 上，但核心的模块组合机制还没验证 |
| **解决方案** | Phase 1 从本地文件系统开始：一个文件夹放模块的 YAML/JSON 文件，用 git 做版本管理。先验证：<br>1. 模块可以被应用声明为依赖<br>2. 依赖解析能工作<br>3. 扩展点能生效<br>4. 升级检测能运行<br>Web Registry 放到 Phase 2 或 Phase 3。 |
| **修改内容** | 第 7.2.3 节添加 Phase 1a 本地文件系统说明；第 7.1 架构图将 Module Registry 标注为 Phase 2+ |
| **负责人** | |
| **截止日期** | |
| **状态** | ✅ 已完成 |

---

## 6. ✅ 护城河分析需要重新聚焦

| 字段 | 内容 |
|---|---|
| **优先级** | P2 |
| **关联章节** | 第 12 节 |
| **问题描述** | 提案列出了 7 个护城河，但部分不够坚固：<br>1. **Business IR**——有价值，但 Retool、Appsmith、Mendix 也有中间表示。<br>2. **企业级运行时**——这是企业软件的入场券，不是差异化。<br>3. **AI 资源治理**——Coze/Dify 已有类似能力。<br>4. **Company Context**——其他平台也有类似概念。<br>5. **服务商生态**——需要冷启动。<br>真正的核心护城河是 **业务模块系统**，但被淹没在其他 6 个中。 |
| **影响** | 对外沟通时焦点模糊，投资人/客户不清楚 ANC 到底强在哪 |
| **解决方案** | 重构第 12 节，把护城河按强度分层：<br>**第一层（真正的壁垒）：** 业务模块系统（模块注册中心 + 版本管理 + 依赖解析 + 扩展点机制 + 飞轮效应）<br>**第二层（重要但可复制）：** Business IR、企业级运行时、AI 资源治理<br>**第三层（需要时间积累）：** Business Skill 积累、Company Context、服务商生态<br>对外沟通时主推第一层。 |
| **修改内容** | 第 12 节重构为三层结构（12.1-12.3），并新增第 12.4 节"对外沟通建议"表格，针对投资人/客户/合作伙伴/技术招聘/竞争对比五类场景给出沟通重点 |
| **负责人** | |
| **截止日期** | |
| **状态** | ✅ 已完成 |

---

## 7. ✅ 缺少：多租户架构决策

| 字段 | 内容 |
|---|---|
| **优先级** | P1 |
| **关联章节** | 第 3.2 节（服务商场景）、第 7.3 节（新增） |
| **问题描述** | 提案没有回答一个关键架构问题：对于服务商模式（一家服务商服务多家客户），每个最终客户是独立的应用实例/部署，还是在服务商账号下的多租户？这个决策影响：<br>1. 数据隔离方案<br>2. 部署架构（单租户部署 vs 多租户共享）<br>3. 版本管理（每个客户独立升级还是统一升级）<br>4. 计费模型 |
| **影响** | 不决策会导致后期架构返工 |
| **解决方案** | 建议 Phase 1 采用 **应用实例模式**（每个客户一个独立的应用，共享平台基础设施，但数据隔离），这样最简单。多租户作为 Phase 3 的优化。需要把这个决策明确写入架构文档。 |
| **修改内容** | 新增第 7.3 节"多租户与部署模式决策"，包含：7.3.1 应用实例模式说明（数据隔离/部署/版本/定制方案）、7.3.2 不做真正多租户的原因、7.3.3 未来演进路径 |
| **负责人** | |
| **截止日期** | |
| **状态** | ✅ 已完成 |

---

## 8. ✅ 缺少：技术栈推荐

| 字段 | 内容 |
|---|---|
| **优先级** | P0 |
| **关联章节** | 第 15 节（新增） |
| **问题描述** | 提案把 "选择技术栈" 列为下一步工作，但技术栈的选择会影响 IR 设计、生成器架构、运行时决策。没有方向性建议会使后续工作发散。 |
| **影响** | 团队在技术选型上花费过多时间；不同人的假设不一致导致返工 |
| **解决方案** | 尽快做出技术栈决策并写入提案。建议讨论以下选项：<br>**前端：** React vs Vue（建议 React，生态更丰富）<br>**后端：** Node.js vs Python vs Go（Node.js 全栈效率高，Go 运行时性能好）<br>**工作流引擎：** Temporal vs 自研轻量引擎（Temporal 成熟但重，自研轻量更灵活）<br>**数据库：** PostgreSQL（结构化数据）+ 什么存 IR（JSONB / SQLite / 文件）<br>**LLM 网关：** LiteLLM vs Portkey vs 自研<br>先在团队内对齐，再更新到 proposal 中。 |
| **修改内容** | 新增第 15 节"技术栈决策"：15.1 后端 Go（含 Go/Rust/Zig 对比表）、15.2 工作流 Temporal（含价值/代价分析）、15.3 其他决策（React/H5/PG/LiteLLM）、15.4 暂不做决策列表；原"下一步工作"移到第 16 节并移除技术栈选型项 |
| **负责人** | |
| **截止日期** | |
| **状态** | ✅ 已完成 |

---

## 9. ✅ 存在 "自底向上" 实现的风险

| 字段 | 内容 |
|---|---|
| **优先级** | P0 |
| **关联章节** | 第 10 节 Phase 1a |
| **问题描述** | 当前方案是先定义 IR → 构建生成器 → 构建运行时。这像编译器的思路，合理，但存在风险：<br>1. 在基础设施上投入数月，用户才能看到东西。<br>2. 生成第一个真实应用时，才发现 IR 设计有遗漏。<br>3. 团队容易陷入抽象优化，推迟端到端验证。 |
| **影响** | 长时间无可见产出；IR 设计缺陷发现得晚，返工成本高 |
| **解决方案** | 采用 **"瘦垂直切片"** 策略，与 IR 工作并行：<br>1. 手写一个资产盘点场景的完整 Business IR。<br>2. 手写一个仅针对该 IR 的生成器（不做泛化）。<br>3. 跑通一个真实应用（Web + H5 扫码）。<br>4. 验证 IR 是否覆盖了所有必要的能力。<br>5. *然后* 泛化 IR 和生成器。<br>这样在 2-4 周内就能看到端到端产物，同时 IR 设计也有真实反馈。 |
| **修改内容** | 第 10 节 Phase 1a 新增"实现策略：瘦垂直切片"说明，包含 5 步操作步骤和预期时间（2-4 周见端到端产物） |
| **负责人** | |
| **截止日期** | |
| **状态** | ✅ 已完成 |

---

## 10. ✅ 缺少：Phase 1 成功标准的可测量指标

| 字段 | 内容 |
|---|---|
| **优先级** | P1 |
| **关联章节** | 第 14 节 |
| **问题描述** | 提案第 14 节列出了 8 个成功标准，但多数是定性的（"能生成"、"能支持"、"能追踪"），缺乏具体的可测量指标。这会导致：<br>1. 无法判断 Phase 1 是否真正完成。<br>2. 团队不知道做到什么程度算"够了"。<br>3. 容易无休止打磨。 |
| **影响** | Phase 1 结束条件不清晰，容易 scope creep |
| **解决方案** | 为每个成功标准补充定量指标。例如：<br>1. 能从自然语言生成结构化业务规格 → **10 种不同的需求描述，8 种以上能正确生成**<br>2. 能生成并运行完整的资产/仓储应用 → **Demo 脚本中的 9 步闭环全部通过**<br>3. 能支持入库、出库、盘点三条核心流程 → **端到端跑通 3 条流程的正向和异常场景**<br>4. 能追踪 Agent Run Log → **每次生成都能完整回放当时的 Prompt、模型、输出**<br>建议为每个标准设定 Pass/Fail 条件。 |
| **修改内容** | 第 14 节将 8 个定性标准升级为表格形式，每个标准增加定量条件和所属 Phase（6 个 Phase 1a + 2 个 Phase 1b） |
| **负责人** | |
| **截止日期** | |
| **状态** | ✅ 已完成 |

---

## 汇总

| # | 问题 | 优先级 | 状态 |
|---|---|---|---|
| 1 | 工程范围被严重低估 | P0 | ✅ |
| 2 | Business IR 过度设计风险 | P0 | ✅ |
| 3 | Agent 架构过于复杂 | P0 | ✅ |
| 4 | AI Resource Registry 对 MVP 太重 | P1 | ✅ |
| 5 | Module Registry UI 对 Phase 1 太重 | P1 | ✅ |
| 6 | 护城河分析需要重新聚焦 | P2 | ✅ |
| 7 | 缺少多租户架构决策 | P1 | ✅ |
| 8 | 缺少技术栈推荐 | P0 | ✅ |
| 9 | "自底向上"实现风险 | P0 | ✅ |
| 10 | 缺少可测量的成功标准 | P1 | ✅ |

**P0 数量：4** → 已全部处理
**P1 数量：4** → 已全部处理
**P2 数量：1** → 已处理
