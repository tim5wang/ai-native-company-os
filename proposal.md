# ANC 产品计划：AI 原生企业业务操作系统

> **版本**: v2.0  
> **日期**: 2026-05-08  
> **定位**: 面向企业数字化负责人和数字化实施服务商的 AI 原生企业业务操作系统  
> **MVP 场景**: 资产盘点 / 仓储出入库

---

## 1. 一句话定位

ANC 是一个 **AI 原生企业业务操作系统**。

它让企业数字化负责人或实施服务商用自然语言描述业务需求，由 Builder Agent 将需求转化为可运行的业务应用、数据模型、页面、流程、权限和审计规则；运行期尽量使用确定性代码和规则执行，Agent 主要负责构建、维护、诊断和持续优化。

简化表达：

> **业务人员描述需求，AI 生成业务系统；系统确定性运行，AI 持续维护。**

---

## 2. 为什么需要 ANC

传统企业数字化不缺工具，缺的是一个能快速把业务流程落成稳定系统的底座。

### 2.1 传统 OA / ERP 的问题

1. **定制慢**  
   业务流程一变，就需要供应商或 IT 团队改表、改页面、改流程、改权限。

2. **成本高**  
   很多中小企业的软件采购成本并不高，真正贵的是实施、二开、维护和反复沟通。

3. **响应慢**  
   业务负责人知道需求，但不会建系统；IT 懂系统，但不一定懂业务细节。

4. **体验差**  
   很多系统只解决“能不能用”，没有解决“业务人员愿不愿意用”。

5. **AI 是外挂**  
   企业里的 AI 往往停留在聊天框、知识库问答或独立 Copilot，无法真正理解业务流程、数据结构、权限边界和运行状态。

### 2.2 当前 AI 工具的问题

ChatGPT、Coze、Dify、n8n、Make 等工具都很有价值，但它们没有完整解决企业业务系统生成问题：

| 类型 | 价值 | 不足 |
|---|---|---|
| Chat Assistant | 易用、灵活、适合问答和生成 | 不直接进入业务系统，缺少流程、权限、审计和运行态 |
| AI 应用平台 | 适合做问答、客服、简单 Agent | 多数运行时依赖 LLM，稳定性和成本不可控 |
| Workflow Builder | 适合确定性自动化 | 需要人先设计流程，不适合从业务需求直接生成系统 |
| 低代码平台 | 能搭表单和流程 | 仍然依赖实施人员配置，AI 原生程度低 |
| 传统 OA / ERP | 企业级能力较完整 | 僵化、定制贵、AI 难以内生集成 |

进一步看，Coze、n8n、Dify 分别代表三类值得学习但不应照搬的能力：

| 产品 | 强项 | ANC 应学习什么 | ANC 不应变成什么 |
|---|---|---|---|
| Coze | Agent 应用开发、Prompt / 插件 / 工作流 / 知识库资源编排 | 统一管理 AI 资源，让 Agent、工具、知识库和工作流有版本、权限和发布机制 | Bot Studio 或客服 Agent 平台 |
| n8n | 节点式自动化、系统集成、执行历史、错误处理 | 确定性流程运行、节点级日志、重试、人工介入、凭据和连接器治理 | 以画布为中心的通用自动化工具 |
| Dify | LLM 应用工程化、RAG、Agentic Workflow、模型和日志管理 | 模型网关、Prompt 版本、RAG 管线、生成结果观测、成本统计 | 只面向问答 / RAG / LLM App 的平台 |
| Marvis | OS 级 AI Agent、多 Agent 协作编排、混合路由成本控制、隐私模式 | 混合路由策略（规则引擎优先，LLM 兜底）降低构建成本；Agent 长任务的进度可视化 UX；能力边界克制 | 桌面操控工具、个人助手、依赖 Agent 动态执行的系统 |

ANC 的切入点不是再做一个聊天框，也不是再做一个流程画布，而是：

> **让 AI 进入企业业务系统的构建、运行和维护链路。**

---

## 3. 目标用户

第一版不直接面向所有企业员工，而是面向两类用户。

### 3.1 企业数字化负责人

包括企业内部的信息化负责人、运营负责人、管理者或懂业务的数字化推进者。

他们的需求：

- 快速把线下流程变成线上应用；
- 降低对外部软件厂商和内部开发资源的依赖；
- 能看清业务流程、数据流、权限和运行状态；
- 业务变化后可以快速调整系统。

### 3.2 数字化实施服务商

包括为传统企业做 OA、ERP、进销存、仓储、制造、零售数字化的服务商。

他们的需求：

- 缩短交付周期；
- 降低定制开发成本；
- 将行业经验沉淀为可复用模板；
- 同时服务更多客户；
- 通过 AI 辅助需求澄清、方案生成、实施和运维。

---

## 4. 核心产品理念

### 4.1 构建期 AI，运行期确定性

ANC 不追求让 LLM 在每一次业务流程中临场判断。

更适合企业级场景的方式是：

```text
构建期：
业务需求 → Builder Agent 澄清 → 生成模型/页面/流程/权限/代码 → 测试 → 发布

运行期：
用户操作 / 业务事件 → 确定性代码和规则执行 → 日志 / 审计 / 通知 / 状态更新
```

这样可以降低：

- LLM 幻觉风险；
- 运行成本不确定性；
- 审计困难；
- 延迟和稳定性问题。

Agent 的主要职责是：

- 帮人构建系统；
- 帮人理解系统；
- 帮人修改系统；
- 帮人定位问题；
- 帮人持续优化系统。

### 4.2 产品体验像 Workspace Agent，运行机制像 Workflow Engine

用户体验上，ANC 应该像一个懂业务的数字化顾问：

- 用户用自然语言描述目标；
- Agent 主动追问缺失信息；
- Agent 给出业务对象、流程、权限和页面方案；
- 用户确认后发布应用。

运行机制上，ANC 应该像一个可靠的企业级工作流和应用平台：

- 流程可视化；
- 数据结构清晰；
- 权限可控；
- 日志可查；
- 版本可回滚；
- 异常可诊断；
- 成本可预测。

这意味着 ANC 的设计原则是：**体验上吸收 Coze / Dify 的自然语言构建能力，运行上吸收 n8n 的确定性执行和可观测能力，工程上吸收 Dify 的 LLMOps 能力，但最终产物必须是企业业务系统，而不是 Agent 对话应用或自动化脚本。**

### 4.3 可复用执行，而不是 Prompt 模板

ANC 要沉淀的不是提示词，而是可运行的业务资产。

建议将这类资产定义为 **Business Skill**。

一个 Business Skill 包含：

- 数据模型；
- 表单页面；
- 列表 / 详情 / 看板页面；
- 工作流；
- 角色和权限；
- 通知规则；
- 外部连接器；
- 审计规则；
- 测试用例；
- 运维策略；
- 版本历史。

例如：

- 资产盘点 Skill；
- 仓储入库 Skill；
- 仓储出库 Skill；
- 采购审批 Skill；
- 报销 Skill；
- 员工入职 Skill；
- 合同归档 Skill。

这些 Skill 可以被 Builder Agent 生成、复用、修改和组合。

---

### 4.4 业务模块系统 — 跨项目复用的核心基础设施

仅有行业模板是不够的。模板是完整的预设方案，但一个企业级系统的构建通常不是"套模板"，而是"拼积木"。

ANC 需要一套 **系统化的业务模块管理机制**，让每一次交付都可以站在前一次的肩膀上，而不是从零生成。

#### 4.4.1 什么是业务模块（Business Module）

一个 Business Module 是**比 Business Skill 更小、更通用、可独立版本化的业务能力单元**。

以一个"资产盘点"场景为例，它由多个模块组合而成：

```text
资产盘点应用
├── 物料模型模块 (v1.2)     ← 通用的物料/资产数据模型
├── 仓库模型模块 (v1.0)     ← 通用的仓库/位置数据模型
├── 入库流程模块 (v2.1)     ← 入库单 + 审批 + 库存更新
├── 出库流程模块 (v2.0)     ← 出库单 + 审批 + 库存扣减
├── 盘点流程模块 (v1.3)     ← 盘点任务 + 差异处理
├── 库存流水模块 (v1.1)     ← 统一的库存流水记录能力
├── 扫码操作模块 (v1.0)     ← 扫码识别 + 动作选择
├── 权限模板模块 (v1.0)     ← 仓库/资产角色的预设权限
└── 通知模块 (v1.2)         ← 审批通知 + 异常通知
```

关键区别：

| | 行业模板 / Business Skill | Business Module |
|---|---|---|
| 粒度 | 完整场景，偏大 | 单一业务能力，偏小 |
| 复用方式 | 整体使用，调整困难 | 组合使用，按需选取 |
| 版本管理 | 应用版本 | 独立语义化版本 |
| 依赖关系 | 无显式依赖 | 可声明依赖其他模块 |
| 定制方式 | 修改模板副本 | 继承 + 覆写 + 配置 |

#### 4.4.2 模块的版本管理

每个 Business Module 遵循 **语义化版本**（SemVer）：

```
MAJOR.MINOR.PATCH
```

- **MAJOR**: 不兼容的变更（数据模型、API 签名变化）
- **MINOR**: 向下兼容的新增能力（新字段、新流程分支、新通知规则）
- **PATCH**: 向下兼容的修复（bug 修复、文案调整、性能优化）

当一个应用声明了模块依赖：

```yaml
modules:
  - name: inventory.warehouse-model
    version: "1.x"          # 允许 1.x 范围内自动升级 MINOR 和 PATCH
    source: registry.anc.dev
  - name: inventory.inbound-flow
    version: ">=2.0 <3.0"
    source: registry.anc.dev
  - name: inventory.outbound-flow
    version: "2.1"          # 锁定精确版本
    customizations:
      - path: approval-rules
        action: override
        value: "金额超过 5000 元需总监审批"
```

#### 4.4.3 模块的组合与定制

模块不是黑盒。一个 Business Module 在 Business IR 层面暴露明确的**扩展点**：

| 扩展点类型 | 说明 | 示例 |
|---|---|---|
| `entity.extend` | 向数据模型追加字段 | 给物料模型追加"序列号"字段 |
| `entity.override` | 覆写字段定义 | 将物料编码长度从 20 改为 50 |
| `flow.extend` | 在流程中插入新节点 | 出库流程中插入"领用人确认"节点 |
| `flow.override` | 覆写流程分支条件 | 修改审批金额阈值 |
| `page.extend` | 在页面中追加区块 | 物料详情页追加"维修记录"区块 |
| `permission.extend` | 追加新的角色和权限 | 追加"质检员"角色 |
| `notification.extend` | 追加新的通知规则 | 库存低于下限时通知采购 |

定制产生的是**新版本的应用实例**，而不是修改原始模块。当上游模块发布新版本时，系统在允许范围内自动合并，冲突时提示用户决策。

#### 4.4.4 模块注册中心（Module Registry）

Module Registry 是模块的中央仓库，类似 npm、Docker Hub 或 PyPI。

功能包括：

- **发布**：模块作者发布新版本，附带变更日志和兼容性声明；
- **发现**：按行业、场景、能力搜索模块；
- **依赖分析**：自动解析模块间的依赖关系，检测版本冲突；
- **升级检测**：检测已使用模块是否有新版本，分析升级影响范围；
- **使用统计**：每个模块被多少应用引用、升级成功率、常见定制模式。

对于数字化实施服务商来说，Module Registry 是他们**沉淀行业经验的核心资产**。每交付一个客户，不是交付完就结束了，而是通过定制产生的模块升级版本，反向丰富 Registry，让下一个客户的交付更快、成本更低。

#### 4.4.5 模块系统的降本逻辑

模块系统带来的不仅是复用便利性，更是**构建阶段 LLM 调用成本的直接降低**。

当 Builder Agent 为一个新客户构建系统时：

| 步骤 | 无模块系统 | 有模块系统 |
|---|---|---|
| 需求澄清 | LLM 从零理解业务 → 多次对话 | LLM 参照已有模块快速匹配 → 更少对话 |
| 数据模型生成 | LLM 生成整个数据模型（大量 token） | 复用模块模型 + 少量增量生成 |
| 页面生成 | LLM 生成全部页面 | 复用模块页面模板 + 定制 |
| 流程生成 | LLM 生成完整流程 | 复用模块流程 + 参数化调整 |
| 权限生成 | LLM 生成全部权限矩阵 | 复用权限模板 + 按需扩展 |
| 测试生成 | LLM 生成全套测试 | 复用模块测试 + 增量测试 |
| **合计 LLM 成本** | **高（每次从零开始）** | **低 60-80%（主要靠组合和定制）** |

关键洞察：**模块复用越充分，Builder Agent 的 LLM 调用就越少，构建速度越快，成本越低，质量也越稳定。**

#### 4.4.6 模块的完整生命周期

```text
创建（Create）
  │
  ▼
发布（Publish）→ Module Registry
  │
  ▼
使用（Use）→ 被某个应用声明为依赖
  │
  ├──→ 定制（Customize）→ 应用实例获得定制配置
  │
  ├──→ 升级检测（Check Upgrade）→ 有新版本时通知
  │
  ├──→ 升级（Upgrade）→ 自动合并或提示冲突
  │
  └──→ 反馈优化（Feedback）→ 使用数据反向改进模块
  │
  ▼
废弃（Deprecate）→ 标记不再推荐使用
  │
  ▼
退役（Retire）→ 从 registry 移除或标记为只读
```

这比"复制模板 → 改模板"的工作模式要系统得多。它让模块成为**可管理、可追踪、可演进**的业务资产。

### 4.5 AI 资源治理与运行可观测

ANC 不能只管理业务模块，还要管理 AI 资源和运行资产。否则系统会退化成一堆无法审计的 Prompt、插件、工作流和临时代码。

建议将平台资产分成三层：

| 资产类型 | 说明 | 借鉴来源 |
|---|---|---|
| Business Module | 数据模型、页面、表单、工作流、权限、通知、审计、测试等业务能力 | ANC 自身的核心壁垒 |
| AI Resource | Prompt、模型配置、知识库、工具、连接器、Agent 配置、RAG 管线 | Coze / Dify |
| Runtime Artifact | 数据库表、API、页面包、流程实例、权限策略、日志、发布版本 | n8n / 企业应用平台 |

这些资产都应具备：

- 版本管理；
- 依赖声明；
- 权限控制；
- 发布审批；
- 运行日志；
- 成本统计；
- 失败追踪；
- 回滚能力。

运行可观测性至少应覆盖三类日志：

| 日志类型 | 记录内容 | 用途 |
|---|---|---|
| App Run Log | 用户操作、数据变更、权限判断、业务异常 | 审计、追责、问题定位 |
| Workflow Run Log | 节点输入输出、状态流转、审批轨迹、重试、人工介入 | 流程诊断、回放、优化 |
| Agent Run Log | 需求澄清、模块匹配、Prompt、工具调用、生成结果、成本、人工确认 | 生成质量评估、Ops Agent 诊断、版本回归 |

这是 ANC 与普通 AI 应用平台的重要区别：AI 不只是生成内容，还必须进入企业级治理链路。

---

## 5. MVP 场景：资产盘点 / 仓储出入库

第一阶段选择 **资产盘点 / 仓储出入库** 作为 MVP 场景。

原因：

1. 它比普通审批流更能体现“生成业务应用”，而不只是“生成流程”。
2. 它包含企业数字化的典型要素：数据模型、移动端操作、后台管理、权限、流程、日志、审计。
3. 它适合传统企业，尤其是制造、贸易、零售、仓储、物业、学校、医院等场景。
4. 它容易展示 AI 从需求到系统的完整构建链路。

### 5.1 MVP 要解决的业务问题

企业中常见的资产和库存管理问题：

- 资产台账分散在 Excel；
- 入库、出库、领用、归还记录不完整；
- 盘点依赖人工登记；
- 实物与系统数据不一致；
- 异常缺少追踪；
- 管理者无法实时查看库存和资产状态；
- 每个企业字段、流程、权限都不同，标准软件很难完全适配。

### 5.2 MVP 应支持的核心对象

第一版建议支持以下业务对象：

1. **资产 / 物料**
   - 名称
   - 编码
   - 分类
   - 规格型号
   - 单位
   - 状态
   - 所属仓库 / 位置
   - 负责人
   - 二维码 / 条码

2. **仓库 / 存放位置**
   - 仓库名称
   - 区域
   - 货架 / 房间 / 工位
   - 管理员

3. **入库单**
   - 入库类型
   - 物料明细
   - 数量
   - 经办人
   - 审核人
   - 入库时间

4. **出库单**
   - 出库类型
   - 领用人 / 部门
   - 物料明细
   - 数量
   - 审核状态
   - 出库时间

5. **盘点任务**
   - 盘点范围
   - 盘点负责人
   - 计划时间
   - 实盘数量
   - 账面数量
   - 差异原因
   - 处理状态

6. **库存流水 / 操作日志**
   - 操作类型
   - 操作人
   - 操作对象
   - 操作前后数量
   - 时间
   - 来源单据

### 5.3 MVP 应支持的角色

- 系统管理员；
- 仓库管理员；
- 资产管理员；
- 普通员工 / 领用人；
- 审批人；
- 管理层查看者。

### 5.4 MVP 应生成的应用形态

1. **移动端 / H5 操作端**
   - 扫码查看资产；
   - 扫码入库；
   - 扫码出库；
   - 盘点录入；
   - 异常上报。

2. **Web 管理后台**
   - 资产 / 物料台账；
   - 仓库和位置管理；
   - 入库单管理；
   - 出库单管理；
   - 盘点任务管理；
   - 库存流水；
   - 异常差异处理；
   - 数据看板。

3. **Builder Agent 对话入口**
   - 需求录入；
   - 需求澄清；
   - 方案确认；
   - 应用生成；
   - 修改需求；
   - 版本发布。

### 5.5 MVP 演示闭环

MVP 最重要的不是功能数量，而是证明以下闭环可行：

```text
1. 用户描述需求
   “我们需要一个仓库出入库和资产盘点系统，支持扫码、审批和库存流水。”

2. Builder Agent 澄清需求
   - 管理哪些物品？
   - 是否需要多仓库？
   - 入库是否需要审批？
   - 出库是否需要审批？
   - 谁可以盘点？
   - 是否需要扫码？
   - 是否需要库存预警？

3. 生成结构化规格
   - 数据模型
   - 页面列表
   - 流程定义
   - 权限矩阵
   - 通知规则
   - 测试用例

4. 用户确认

5. 系统生成并发布应用
   - H5 操作端
   - Web 管理后台
   - 工作流
   - 数据表
   - 权限配置
   - 日志审计

6. 用户运行应用
   - 创建物料
   - 扫码入库
   - 发起出库
   - 审批出库
   - 发起盘点
   - 处理盘点差异

7. 用户提出修改
   “出库超过 5000 元需要部门负责人审批，否则仓库管理员直接确认。”

8. Agent 分析影响并生成变更
   - 修改流程规则
   - 更新权限
   - 生成测试
   - 创建新版本

9. 用户预览并发布
```

---

## 6. 第一版产品范围

### 6.1 必须做

> **说明**：以下内容按优先级分为两阶段。**Phase 1a（核心 MVP）**聚焦跑通 Builder Agent → 最小 IR → App Generator → 基础运行的关键闭环。**Phase 1b（增强 MVP）**在其之上补充权限、日志审计界面、版本管理等能力。

#### Phase 1a 核心（先跑通端到端闭环）

1. **Builder Agent**
   - 自然语言需求输入；
   - 需求澄清；
   - 生成结构化业务规格；
   - 生成应用方案；
   - 支持用户修改需求。

2. **业务中间表示，Business IR（最小子集）**
   - 数据模型定义（Entities、Fields、Relations）；
   - 页面定义（Pages、Forms）；
   - 流程定义（Workflows + 状态流转）；
   - 角色与页面权限定义（Roles、Permissions）；
   - 模块依赖声明（ModuleDependencies + ModuleLock）。
   - *暂不包含*：Notifications、AuditRules、Tests、AIResourceDependencies、RuntimePolicies、Versions。

3. **应用生成器**
   - 根据 Business IR 生成可运行应用；
   - 支持 Web 管理后台；
   - 支持移动端 / H5 操作页；
   - 支持表单、列表、详情、基础看板。

4. **工作流引擎（基础版）**
   - 支持提交、审批、驳回、完成；
   - 支持简单条件分支；
   - 支持角色节点；
   - 支持状态流转；
   - 支持流程日志输出（写入 Workflow Run Log）。

5. **最小日志记录**
   - App Run Log（写入数据库或文件，无前端展示）；
   - Workflow Run Log（写入数据库或文件）；
   - Agent Run Log（每次生成记录 Prompt、模型、输出，写入 generation-manifest.json）。

6. **最小权限**
   - 用户与角色；
   - 页面级权限（Phase 1a 不做数据级和操作级权限）。

#### Phase 1b 增强（在核心闭环基础上补充）

7. **完整的日志与审计**
   - 操作日志管理界面；
   - 数据变更记录；
   - Agent 生成记录查询；
   - 版本记录查询。

8. **完整的权限系统**
   - 部门管理；
   - 数据权限；
   - 操作权限。

9. **版本管理**
   - 应用版本记录；
   - 变更说明；
   - 发布记录；
   - 基础回滚能力。

10. **AI 资源治理（轻量版）**
    - 每次生成时将使用的 Prompt 模板 ID、模型名称、知识库 ID 写入 `generation-manifest.json`；
    - 完整的 Registry UI 和版本管理放到 Phase 3。

### 6.2 暂时不做

第一版不要做太多，否则会失焦。

**整个 Phase 1 都不做：**

- 通用 OA 全家桶；
- 复杂 ERP；
- 财务核算；
- 复杂 BI；
- 多租户计费系统；
- 插件市场；
- 大规模连接器生态；
- 完全自主的无人值守修改生产系统；
- 复杂流程画布编辑器；
- 通用 Bot Studio；
- 通用 RAG 应用平台；
- 以流程画布为主的 n8n / Make 替代品。

**Phase 1a 核心 MVP 暂时不做（放到 Phase 1b 或 Phase 2）：**

- 数据级和操作级权限（Phase 1a 只做页面级权限）；
- 版本管理 UI 和回滚 UI（Phase 1a 只保留 `generation-manifest.json` 记录）；
- AI Resource Registry UI（Phase 1a 只记录 generation-manifest.json）；
- 日志查询和管理界面（Phase 1a 日志先写入数据库/文件，不建前端）；
- 审计规则、测试用例的 IR 定义；
- 通知系统的 IR 定义和生成（Phase 1a 通知写固定逻辑）。

流程画布可以有只读或轻编辑能力，但不应成为 MVP 的主入口。MVP 的主入口应该是 Builder Agent。

---

## 7. 产品架构

### 7.1 总体结构

```text
用户 / 实施顾问
      ↓
Builder Agent
      ↓
结构化需求规格 Spec
      ↓
                              ← 本地模块文件（Phase 1a）→ Module Registry（Phase 2+）
Business IR（组装时引用模块实例）
      ↓
应用生成器 / 流程生成器 / 权限生成器 / 测试生成器
      ↓
Compiled Business App（含 module-lock.json + generation-manifest.json）
      ↓
确定性运行时
      ↓
App Run Log / Workflow Run Log / Agent Run Log / 审计 / 监控 / 反馈
      ↓
Ops Agent / Builder Agent 持续优化
```

### 7.2 核心模块

#### 7.2.1 Builder Agent

负责把自然语言业务需求变成结构化系统设计。

输入：

- 用户描述；
- 行业模板；
- **本地模块文件（Phase 1a 从本地文件系统读取已发布模块；Phase 2+ 从 Module Registry 获取）**；
- **generation-manifest.json（Phase 1a 记录每次生成使用的 Prompt 模版 ID、模型名称、知识库 ID；Phase 3 升级为完整的 AI Resource Registry）**；
- 现有数据表；
- 企业组织结构；
- 历史应用配置；
- 用户反馈。

输出：

- 需求规格；
- 数据模型；
- **模块依赖声明（哪些模块 + 版本范围 + 定制策略）**；
- 页面方案；
- 流程方案；
- 权限矩阵；
- 测试用例；
- AI 资源依赖；
- 发布计划。

#### 7.2.2 Business IR

Business IR 是 ANC 的关键底层设计。

它是业务系统的中间表示，用来承接 Agent 生成结果，并驱动后续应用生成和运行。

Business IR 按阶段扩展。Phase 1a 先实现最小核心子集，后续阶段再加入更多能力。

#### Phase 1a 最小核心 IR

```text
App
├── Entities          数据对象
├── Fields            字段
├── Relations         关联关系
├── Pages             页面
├── Forms             表单
├── Workflows         流程（含 StatusTransitions）
├── Roles             角色
├── Permissions       页面级权限
├── ModuleDependencies   模块依赖声明（模块名 + 版本范围 + 定制覆盖）
└── ModuleLock           模块依赖锁定文件（锁定实际使用的精确版本）
```

#### Phase 1b / Phase 2 扩展 IR

```text
├── Notifications     通知规则
├── AuditRules        审计规则
├── Tests             测试用例
├── AIResourceDependencies ← AI 资源依赖（Prompt / 模型 / 工具 / 知识库 / 连接器）
├── RuntimePolicies       ← 运行策略（重试 / 人工介入 / 成本阈值 / 审计级别）
├── Versions          版本
└── AIResourceLock       ← AI 资源锁定文件（锁定生成和诊断使用的资源版本）
```

Business IR 的价值：

- 让 AI 输出可检查；
- 让应用生成可控；
- **让模块组合可描述、可追踪**；
- 让运行时确定性执行；
- 让版本 diff 可读；
- 让系统可审计、可回滚、可维护。

#### 7.2.3 Module Registry（模块注册中心）

Module Registry 是 ANC 的业务模块仓库，是整个复用体系的基石。

> **Phase 1a**：使用**本地文件系统**作为 Registry。模块以 YAML/JSON 文件存放在项目目录中，用 git 做版本管理。不建 Web UI、不建搜索、不用数据库。目标是在 Phase 1a 验证模块的组合、依赖解析、扩展点等核心机制。
> **Phase 2+**：升级为正式的 Web Registry（详见下方设计）。

未来 Module Registry 的内部结构：

```text
Module Registry
├── 模块元数据
│   ├── 名称 / 描述 / 标签
│   ├── 作者 / 组织
│   ├── 行业 / 场景分类
│   ├── 依赖声明
│   └── 兼容性声明
│
├── 版本清单
│   ├── SemVer 版本号
│   ├── CHANGELOG
│   ├── BREAKING CHANGES 说明
│   ├── Business IR 快照
│   └── 升级迁移指南
│
├── 使用统计
│   ├── 被引用次数
│   ├── 升级成功率
│   ├── 常见定制模式
│   └── 问题报告
│
└── 访问控制
    ├── 公开模块（所有人可用）
    ├── 组织私有模块（仅内部可用）
    └── 付费模块（市场销售）
```

#### 7.2.4 AI Resource Registry

AI Resource Registry 管理 Builder Agent、Ops Agent 和运行时可能使用的 AI 资源。

> **Phase 1a**：**不建 AI Resource Registry**。每次生成时将使用的 Prompt 模版 ID、模型名称、知识库 ID 写入 `generation-manifest.json`，与应用版本绑定。仅保证可追溯，不做版本管理、不做 UI、不做成本统计。
> **Phase 1b**：升级为结构化的 AI 资源记录，支持版本管理和基本查询。
> **Phase 3**：升级为完整的 AI Resource Registry（详见下方设计）。

它不是一个单独的 AI 应用市场，而是 ANC 内部的工程化治理层。

未来 AI Resource Registry 的资源类型包括：

- Prompt 和系统指令；
- 模型配置和路由策略；
- 知识库和检索管线；
- 工具和连接器；
- Agent 配置；
- 评估样例和生成质量指标；
- 成本、延迟和失败统计。

未来的关键要求：

- 每次生成、诊断、修改都能追溯到使用了哪个 Prompt、模型、知识库和工具；
- AI 资源变更必须有版本记录，避免同一个业务需求在不同时间生成出不可解释的差异；
- 高成本、高风险工具调用需要策略限制和人工确认；
- 资源可以被 Business Module 引用，但不能替代 Business Module。

#### 7.2.5 Compiled Business App

Compiled Business App 是由 Business IR 生成的可运行业务应用。

它不是 prompt，也不是临时 Agent 任务，而是包含完整运行资产：

- 数据库表；
- API；
- 页面；
- 流程；
- 权限；
- 测试；
- 日志；
- 部署配置；
- **module-lock.json（模块依赖锁定文件，保证可重现构建）**；
- **ai-resource-lock.json（AI 资源锁定文件，保证生成和诊断过程可追溯）**。

#### 7.2.6 确定性运行时

运行时负责稳定执行业务流程。

能力包括：

- 表单提交；
- 数据校验；
- 状态流转；
- 审批；
- 通知；
- 权限判断；
- 日志记录；
- 异常处理；
- 重试策略；
- 人工介入；
- 执行回放。

运行时原则：

- 优先规则和代码；
- 必要时才调用 LLM；
- 所有 LLM 参与都必须可记录、可解释、可追踪；
- 高风险操作必须人工确认。

#### 7.2.7 Ops Agent

Ops Agent 不直接替人改生产系统，而是先做诊断和建议。

能力包括：

- 分析错误日志；
- 发现流程卡点；
- 识别异常数据；
- 解释系统行为；
- 生成修复建议；
- 生成变更草案；
- 辅助测试和回归。

### 7.3 多租户与部署模式决策

> **Phase 1 决策**：采用**应用实例模式**，即每个客户拥有独立的应用实例。

#### 7.3.1 应用实例模式

| 维度 | 方案 |
|---|---|
| 数据隔离 | 每个客户独立数据库 schema 或独立数据库 |
| 部署方式 | 共享平台基础设施 + 每个客户独立的应用实例 |
| 版本管理 | 每个客户独立升级，互不影响 |
| 定制隔离 | 一个客户的定制不影响其他客户 |
| 复杂度 | 低——不需要实现多租户路由、租户上下文传递等机制 |
| 适用场景 | Phase 1 验证阶段 + 中小规模客户 |

#### 7.3.2 为什么 Phase 1 不做真正多租户

- 多租户需要在数据隔离、租户路由、资源配额、计费等方面投入大量工程；
- Phase 1 的核心目标是验证"从自然语言到可运行应用的端到端闭环"，多租户会分散精力；
- 服务商在 Phase 1 可以通过部署多个实例的方式来服务多个客户；
- 真正的多租户架构（共享实例 + 租户隔离）放到 Phase 3 再考虑。

#### 7.3.3 未来多租户演进的路径

```text
Phase 1: 每个客户独立实例，共享平台基础设施
  ↓
Phase 2: 同一服务商账号下管理多个客户项目（每个项目仍独立数据）
  ↓
Phase 3: 真正的多租户运行时（共享计算 + 租户级数据隔离）
```

---

## 8. Agent 设计

> **设计原则**：减少独立 Agent 数量，降低 LLM 调用链长度和上下文碎片问题。
> **方案**：Builder Agent（唯一面向用户的 Agent）负责对话与规划，将技术子任务委托给**工具函数（Tools/Functions）**而非独立 Agent。Ops Agent 仅用于运维诊断，在 Phase 2 之后启用。

### 8.1 Builder Agent（唯一面向用户的 Agent）

Builder Agent 是 ANC 中唯一直接与用户对话的 LLM Agent。它的职责覆盖从需求澄清到方案生成的全过程，具体技术任务通过调用工具函数完成。

核心职责：

- **需求澄清**：主动追问缺失信息（业务对象、流程规则、审批条件、权限结构等）；
- **方案规划**：根据需求决定使用哪些模块、需要生成哪些增量；
- **任务编排**：调用 Schema Tool、Workflow Tool、UI Tool、Test Tool 生成 IR 片段，并组装为完整方案；
- **变更影响分析**：用户提出修改时，分析影响范围并输出变更 diff。

设计要点：

- Builder Agent **不直接生成代码**，它生成结构化 IR，由下游生成器编译为可运行应用；
- Builder Agent **不单独调 LLM 做子任务**，子任务由工具函数处理（工具函数内部可调用 LLM，也可使用确定性逻辑）；
- 所有工具调用过程写入 Agent Run Log，保证可追溯。

典型对话：

```text
用户：我们想做一个资产盘点和仓库出入库系统。

Builder Agent：
我需要确认几个问题：
1. 管理的是固定资产、耗材，还是两者都有？
2. 是否有多个仓库或存放地点？
3. 入库是否需要审批？
4. 出库是否需要审批？
5. 是否需要员工扫码领用？
6. 盘点差异由谁处理？
7. 是否需要库存下限预警？
```

### 8.2 Builder 工具函数（Tools）

Schema、Workflow、UI、Test 不作为独立 Agent，而是 Builder Agent 可调用的**工具函数**。每个工具函数接收结构化输入，输出 IR 片段。

#### 8.2.1 Schema Tool

职责：

- 根据需求描述生成数据对象（Entities）、字段（Fields）和关联关系（Relations）；
- 识别枚举值和状态机；
- 生成数据校验规则。

调用方式：Builder Agent 在确认了业务对象后调用，返回 `Entities[]`、`Fields[]`、`Relations[]` 片段。

#### 8.2.2 Workflow Tool

职责：

- 生成流程节点和状态流转（StatusTransitions）；
- 生成审批规则和条件分支；
- 生成异常分支；
- 生成 Workflow Run Log 的记录点定义。

调用方式：Builder Agent 在确认了流程需求后调用，返回 `Workflow[]` 片段。

#### 8.2.3 UI Tool

职责：

- 根据数据模型和流程生成表单、列表、详情页定义；
- 生成移动端扫码操作页面定义；
- 生成管理后台导航结构。

调用方式：Builder Agent 在生成数据模型和流程后调用，返回 `Pages[]`、`Forms[]` 片段。

#### 8.2.4 Test Tool

职责：

- 根据业务规则和 IR 生成测试用例；
- 覆盖正常流程、异常流程、权限边界、数据校验。

调用方式：Builder Agent 在生成完整 IR 后调用，输出测试用例定义（Phase 1b 启用）。

> **工具函数 vs 独立 Agent 的关键区别**：工具函数没有独立的对话轮次，它们是一次性调用（call → return），不维护自己的上下文窗口。这显著减少了 LLM token 消耗，降低了编排复杂性，也更容易调试和测试。

### 8.3 Ops Agent（Phase 2+ 启用）

Ops Agent 在 Phase 2 之后引入，不直接面向用户，用于运维场景的诊断和建议。

职责：

- 日志解释（分析 App Run Log / Workflow Run Log / Agent Run Log）；
- 异常诊断；
- 流程瓶颈分析；
- 生成修复建议；
- 生成版本变更草案；
- 识别高成本、低质量或高失败率的 AI 资源；
- 将诊断结果转化为可测试的变更建议。

---

## 9. 关键能力优先级

> **说明**：P0 = Phase 1a 核心 MVP 必须验证的能力；P1 = Phase 1b 增强 MVP 补充的能力；P2 = 长期壁垒。

### P0：Phase 1a 核心 MVP（必须验证）

- 自然语言需求 → 结构化规格；
- 结构化规格 → 数据模型；
- 数据模型 → 页面；
- 结构化规格 → 工作流；
- 角色与页面级权限生成；
- 应用发布；
- 基础日志（App Run Log / Workflow Run Log 写入存储，无前端界面）；
- Agent Run Log 的最小闭环（记录到 generation-manifest.json）；
- **模块定义与发布**（定义 Business Module 结构、发布到本地 Registry）；
- **模块依赖声明**（应用声明依赖的模块和版本范围）；
- **模块组合生成**（Builder Agent 优先引用已有模块，再增量生成缺失部分）。

### P1：Phase 1b 增强 MVP（补充体验）

- 应用修改和版本记录；
- 模板复用；
- 业务规则可视化；
- 简单流程图预览；
- 扫码能力；
- 数据导入导出；
- 企业微信 / 飞书 / 钉钉通知；
- 库存预警；
- 盘点差异报表；
- 完整的日志与审计前端界面；
- 数据级和操作级权限；
- **模块升级检测**（检测已使用模块是否有新版本，分析升级影响）；
- **模块定制扩展点**（entity.extend / flow.override 等扩展机制）；
- **AI 资源锁定**（记录生成过程使用的 Prompt、模型、知识库和工具）；
- Prompt / 模型 / 知识库版本管理；
- 运行成本统计。

### P2：形成壁垒

- Business Skill 市场；
- 行业模板库；
- 连接器生态；
- 运行数据驱动的 Agent 优化；
- Company Context；
- 多应用协同；
- 私有部署；
- LLMOps 质量评估和自动回归；
- 连接器权限、凭据和调用审计体系。

---

## 10. 路线图

### Phase 0：产品和技术设计

目标：明确最小闭环和底层抽象。

产出：

- MVP 业务流程定义；
- Business IR 初版（**含 ModuleDependencies 定义**）；
- **Module Registry 架构设计**；
- **Business Module 规范 v0.1**（模块结构、版本语义、扩展点定义）；
- Agent 分工设计；
- 应用生成架构；
- 运行时架构；
- AI Resource Registry 初版设计；
- App Run Log / Workflow Run Log / Agent Run Log 规范；
- 权限和审计模型；
- Demo 脚本。

### Phase 1：单场景 Demo

> 分为两个子阶段。**Phase 1a** 先跑通核心闭环（约全阶段 60% 的工作量），**Phase 1b** 补充体验和治理能力。

#### Phase 1a：核心 MVP（Builder Agent → 最小 IR → App Generator → 基础运行）

目标：跑通从自然语言到可运行资产盘点应用的端到端最小闭环。

> **实现策略：瘦垂直切片（Thin Vertical Slice）**
> 
> 为了避免"先定义 IR → 构建生成器 → 构建运行时"这种自底向上方法带来的风险（数月无可见产出、IR 设计缺陷发现晚），Phase 1a 采用"瘦垂直切片"策略：
> 
> 1. **手写一个完整的 Business IR**，覆盖资产盘点 / 仓储出入库场景的所有必要字段和结构；
> 2. **手写一个仅针对该 IR 的生成器**（不做泛化，不追求通用性）；
> 3. **跑通一个真实应用**（Web 后台 + H5 扫码端）；
> 4. **验证 IR 是否覆盖了所有必要的能力**，发现遗漏立刻调整 IR schema；
> 5. *然后* **将手写生成器抽象为通用 App Generator**，将手写 IR 反向填充 Builder Agent 的生成模板。
> 
> 这种方式让团队在 **2-4 周内看到端到端产物**，同时 IR 设计获得真实反馈，避免在抽象层过度优化。
> 
> 后续 Phase 1b、Phase 2 的能力都在这个已验证的垂直切片基础上迭代。

能力：

- Builder Agent 对话（需求输入 + 需求澄清）；
- 最小 Business IR（Entities、Fields、Relations、Pages、Forms、Workflows、Roles、Page-level Permissions、ModuleDependencies）；
- 数据模型生成；
- Web 后台生成；
- H5 操作页生成；
- 入库流程；
- 出库流程；
- 盘点流程；
- 角色与页面级权限；
- App Run Log / Workflow Run Log（写入存储，无前端界面）；
- Agent Run Log（写入 generation-manifest.json）；
- **将核心业务能力拆分为独立模块（物料模型、入库流程、出库流程、盘点流程等）**；
- **模块发布到本地 Registry（本地文件系统）**；
- **应用通过模块依赖声明组装**。

#### Phase 1b：增强 MVP（补充完整日志、权限、版本、AI 资源记录）

目标：在核心闭环基础上补充企业级必备的治理和体验能力。

能力：

- 完整的日志与审计管理界面；
- 数据级和操作级权限；
- 版本发布与基础回滚；
- **生成过程写入 AI 资源锁定文件**（记录使用的 Prompt 模板、模型、知识库）；
- Prompt / 模型 / 知识库版本管理（轻量版）；
- 运行成本统计；
- 企业微信 / 飞书 / 钉钉通知；
- 数据导入导出。

### Phase 2：可修改、可发布、可回滚

目标：证明系统不是一次性 Demo，而是可持续演进。

能力：

- 用户提出变更；
- Agent 分析影响范围；
- 生成变更 diff；
- 沙箱预览；
- 测试用例生成；
- 版本发布；
- 回滚；
- 基于执行日志的回归验证；
- Prompt / 模型 / 知识库变更影响分析；
- **模块升级流程**（检测新版本 → 分析兼容性 → 自动合并或提示冲突）；
- **模块定制扩展点生效**（用户可通过扩展点定制模块行为，不修改原始模块）。

### Phase 3：服务商交付工具

目标：让数字化实施服务商可以用 ANC 交付客户项目。

能力：

- 客户项目空间；
- **组织私有 Module Registry**（服务商可以管理自己的模块库）；
- 行业模板；
- 需求访谈辅助；
- 方案自动生成（**优先匹配 Registry 中已有模块，标注复用比例**）；
- 实施文档生成；
- 运维诊断；
- 模板复用；
- **模块复用效率看板**（每个项目的模块复用率、节省的 LLM 调用次数、节省的时间）；
- **AI 资源质量看板**（Prompt、模型、知识库、工具调用的成本、失败率和产出质量）。

### Phase 4：企业业务操作系统

目标：从单应用生成扩展到多应用、多流程、多连接器的企业级平台。

能力：

- Company Context；
- 跨应用流程；
- 连接器生态；
- 统一权限；
- 统一审计；
- 运维 Agent；
- Business Skill 生态；
- **公开 Module Registry 市场**（允许第三方发布和销售模块）；
- **模块升级自动化**（CI/CD 式的一键升级 + 回归测试）；
- **企业级 AI Resource Registry**（组织级 Prompt、模型、知识库、工具和连接器治理）。

---

## 11. 商业模式

### 11.1 早期商业模式

优先服务两类客户：

1. **企业数字化负责人**
   - 按企业订阅；
   - 按应用数量收费；
   - 按用户数量收费；
   - 私有部署单独收费。

2. **数字化实施服务商**
   - 服务商账号订阅；
   - 项目空间收费；
   - 模板库收费；
   - 生成和部署能力收费；
   - 联合交付分成。

### 11.2 长期商业模式

- SaaS 订阅；
- 私有部署；
- 行业模板市场；
- Business Skill 市场；
- 连接器市场；
- 企业运维和支持服务。

---

## 12. 竞争壁垒

ANC 的壁垒不在于"能不能调用 LLM"，而在于以下几个层次。理解不同层次的壁垒强度，有助于聚焦资源和对外沟通。

### 12.1 第一层：真正的核心壁垒

#### 业务模块体系（Business Module System）

这是 ANC 最核心、最难复制的护城河。

IR 解决的是"如何描述一个应用"，模块系统解决的是"如何让多个应用的构建不重复做功"。

- 模块注册中心 + 语义化版本管理 + 依赖解析 + 扩展点机制 + 升级迁移路径，是一项系统工程，需要深入理解企业场景才能构建好；
- **飞轮效应**：每交付一个客户，模块库就丰富一次，下一次交付成本就更低；
- 单纯做"模板"很容易被复制，但有版本管理、依赖解析、扩展点机制和迁移路径的模块体系很难复制；
- 实施服务商在 ANC 上沉淀的私有模块越多，切换成本越高，生态粘性越强。

> 对外沟通时，**建议将业务模块体系作为 ANC 的核心差异化能力来讲述**。

### 12.2 第二层：重要但需持续投入

这些是 ANC 的重要能力，并非独有，但如果在执行上比对手做得更好，可以形成阶段性优势。

#### Business IR

谁能把自然语言需求稳定转成可检查、可运行、可维护的业务中间表示，谁就能控制生成质量。这是必要但不充分的壁垒——Retool、Appsmith、Mendix 等低代码平台也有类似概念，但 ANC 的 IR 面向 AI 生成原生设计，更灵活。

#### 企业级运行时

企业客户需要的不只是生成页面，还需要权限、审计、日志、版本、回滚、数据安全和稳定运行。这是企业软件的入场券，不是差异化优势，但做不好会直接出局。

#### AI 资源治理和 LLMOps

Coze、Dify 已经证明 AI 应用进入生产后需要治理能力。ANC 的做法是把它与业务模块体系绑定，保证每次生成都可追溯。这是加分项但不是独占优势。

### 12.3 第三层：需要时间积累的网络效应

这些优势理论上成立，但需要先达到一定的客户和服务商数量才能生效。

#### Business Skill 积累

每交付一个场景，都可以沉淀为可复用 Skill。随着行业模板增加，交付成本会持续下降。但这需要大量真实交付才能形成规模。

#### Company Context

长期来看，ANC 需要理解企业的组织结构、角色权限、业务对象、流程、数据、文档、日志、用户行为和业务指标。这会形成持续上下文优势——但前提是 ANC 已经在企业内部深度使用。

#### 实施服务商生态

数字化实施服务商可以把行业经验沉淀到 ANC 上，ANC 则帮助他们提升交付效率。这个生态一旦形成，会比单纯 SaaS 更有扩展性。但存在冷启动问题——**先有客户，才有生态**。

### 12.4 对外沟通建议

| 场景 | 建议重点 |
|---|---|
| 投资人沟通 | 第一层（模块系统 + 飞轮效应），附带第三层（生态网络效应）的长期想象 |
| 客户沟通 | 第二层（能跑、能审计、能回滚），用第一层（模块系统）解释为什么交付快、成本低 |
| 合作伙伴沟通 | 第一层（模块系统如何帮他们沉淀资产） + 第三层（生态共建） |
| 技术招聘 | 第一层 + 第二层的系统工程挑战 |
| 竞争对比 | 第一层（模块系统是 Coze/Dify/n8n 都没有的）作为核心差异点 |

---

## 13. 风险与约束

### 13.1 主要风险

| 风险 | 说明 | 应对 |
|---|---|---|
| 需求不清 | 用户自然语言描述可能缺失关键规则 | Builder Agent 必须强制澄清 |
| 生成质量不稳定 | Agent 可能生成不合理模型或流程 | Business IR 校验 + 测试 + 人工确认 |
| 运行事故 | 自动生成系统可能影响真实业务 | 沙箱预览 + 灰度发布 + 回滚 |
| 权限错误 | 企业系统中权限错误风险高 | 权限矩阵可视化 + 权限测试 |
| 范围失控 | 企业业务系统范围极大 | MVP 聚焦资产盘点 / 仓储出入库 |
| 被认为只是低代码 | 市场认知可能偏差 | 强调 Agent 构建、Business IR、确定性运行和自维护 |
| 被认为只是 Bot / Workflow / RAG 平台 | Coze、n8n、Dify 已经覆盖相邻市场 | 明确 ANC 的最终产物是可运行、可治理、可演进的业务系统 |
| AI 资源漂移 | Prompt、模型、知识库变化会导致生成结果不可复现 | AI Resource Registry + ai-resource-lock.json + 生成日志 |
| 运行过程不可诊断 | 只记录业务数据，不记录流程和 Agent 执行过程 | App Run Log + Workflow Run Log + Agent Run Log |
| 服务商使用门槛 | 实施服务商需要学习新方法 | 提供模板、访谈脚本、交付文档生成 |

### 13.2 产品原则

1. **AI 生成，但人确认后发布。**
2. **运行期优先确定性，不滥用 LLM。**
3. **所有变更必须有版本记录。**
4. **所有关键操作必须可审计。**
5. **先做好一个场景，再扩展多个行业。**
6. **优先服务懂业务流程的人，而不是追求所有员工自助搭建。**
7. **AI 资源必须可追溯，不能让 Prompt、模型和工具调用成为黑盒。**
8. **流程画布、RAG、Bot 能力都只是支撑能力，不是 MVP 的主入口。**

---

## 14. 当前最小产品定义

第一版 ANC 可以定义为：

> 一个面向企业数字化负责人和数字化实施服务商的 AI 应用生成与业务流程运行平台。用户通过 Builder Agent 描述资产盘点 / 仓储出入库需求，系统生成可运行的 Web 管理后台、H5 扫码操作端、数据模型、流程、权限、日志和审计，并记录生成过程使用的模块、Prompt、模型、知识库和工具，支持后续通过自然语言修改、影响分析、回归验证和版本发布。

第一版成功标准（含定量条件）：

| # | 标准 | 定量条件 | Phase |
|---|---|---|---|
| 1 | 能从自然语言生成结构化业务规格 | **10 种不同的需求描述（覆盖不同字段、流程、权限组合），8 种以上能正确生成完整 IR** | Phase 1a |
| 2 | 能生成并运行一个完整的资产/仓储应用 | **Demo 脚本 9 步闭环全部通过，从需求描述到可操作的应用界面** | Phase 1a |
| 3 | 能支持入库、出库、盘点三条核心流程 | **每条流程的端到端正向场景 + 至少 1 个异常场景（如审批驳回、盘点差异）跑通** | Phase 1a |
| 4 | 能生成角色与页面级权限 | **管理员、仓库管理员、普通员工三个角色看到不同页面和操作** | Phase 1a |
| 5 | 能追踪 App Run Log 和 Workflow Run Log | **每次操作和流程节点转换都能在日志中查到，且日志字段完整** | Phase 1a |
| 6 | 能追踪 Agent Run Log | **每次生成都能完整回放当时的 Prompt 模版 ID、模型名称、输入输出，记录在 generation-manifest.json 中** | Phase 1a |
| 7 | 能根据用户反馈修改应用并生成新版本 | **至少 2 种类型的修改（修改流程规则 + 修改数据模型）能正确生成变更 diff 并发布** | Phase 1b |
| 8 | 能锁定生成过程使用的模块 | **应用通过模块依赖声明组装，module-lock.json 完整记录每个模块的精确版本** | Phase 1a |
| 9 | 能让数字化实施人员看到交付效率提升 | **与 2-3 个服务商做一次模拟交付，记录从需求到应用生成的时间，并收集定性反馈** | Phase 1b |

> 定量条件的意义：让团队知道做到什么程度算"够了"，避免无休止打磨。每个条件可精确验证（Pass / Fail）。

---

## 15. 技术栈决策

> 以下决策基于 2026-06-02 评审讨论确定，供团队对齐。

### 15.1 后端语言：Go

| 决策项 | 选择 | 理由 |
|---|---|---|
| **后端主语言** | **Go** | 见下方对比 |
| **IR 核心库** | Go（暂不考虑混合语言方案） | Phase 1 不宜引入跨语言构建复杂度 |
| **备选方案** | 如果未来 IR 处理成为性能瓶颈，可考虑将 IR 核心抽取为 Rust 动态库 | Phase 3+ 再做评估 |

**Go vs Rust vs Zig 对比（针对 ANC 场景）：**

| 维度 | Go | Rust | Zig |
|---|---|---|---|
| IR 处理（代数类型） | ⚠️ interface + type switch 可接受 | ✅ enum + match 最佳 | ❌ 无原生代数类型 |
| 工作流引擎集成 | ✅ Temporal 原生 Go SDK | ⚠️ SDK 功能不全 | ❌ 不支持 |
| Web 框架生态 | ✅ Gin / Echo / Fiber | ✅ Axum / Actix-web | ❌ 起步阶段 |
| ORM / 数据层 | ✅ GORM / Ent | ⚠️ Diesel / SQLx 较复杂 | ❌ 无成熟方案 |
| LLM 调用 | ✅ 标准 HTTP | ⚠️ 需自己封装 | ❌ 需自己封装 |
| 编译速度 | ✅ 秒级 | ❌ 中大型项目 5-15 分钟 | ✅ 极快 |
| 学习曲线 | ✅ 低 | 🔴 陡峭 | ⚠️ 语法简洁但概念独特 |
| 招聘难度 | ✅ 容易 | 🔴 困难 | 🔴 几乎不可能 |
| 运行时性能 | ⚠️ GC 有停顿，企业场景无影响 | ✅ 零成本抽象 | ✅ 零成本抽象 |
| 语言稳定性 | ✅ 稳定 | ✅ 稳定但生态仍在变 | ⚠️ pre-1.0 |

**结论：Go 是 ANC 当前最优选。** 它在大盘基础设施（Web、ORM、gRPC、工作流引擎、LLM 调用）上都有成熟方案，能最小化 PHASE 1 的工程风险，快速跑通端到端闭环。Rust 的 IR 处理优势存在，但不值得为此牺牲整个生态和团队扩展性。

### 15.2 工作流引擎：Temporal

| 决策项 | 选择 | 理由 |
|---|---|---|
| **工作流引擎** | **Temporal** | 见下方分析 |
| Phase 1 部署 | Docker Compose（本地开发）+ Temporal Cloud（生产） | 省去自建 Temporal Server 集群的运维成本 |
| 备选方案 | 简单状态机 + 数据库轮询 | 仅当团队评估 Temporal 学习成本过高时考虑，但需预留迁移路径 |

**Temporal 对 ANC 的价值：**

| 能力 | 自己实现 | Temporal |
|---|---|---|
| 长时运行 + 宕机恢复 | 需写持久化 + 恢复逻辑（数周） | 内置，配置即可 |
| 重试 / 超时 | 重试策略 + 退避算法 + 死信队列 | `RetryPolicy` 一行配置 |
| 人工介入（驳回/转办） | Signal 机制 + 等待队列 | `workflow.Selector` 原生支持 |
| Workflow Run Log | 自建日志表 + 查询 API | Event History 自带，可同步到自有存储 |
| 流程版本兼容 | 自建版本路由 + 迁移 | `GetVersion` 原生支持 |
| ​**合计节省** | **数月的引擎基础设施工作** | **开箱即用，聚焦业务逻辑** |

**Temporal 的代价：**

- **基础设施复杂**：多一套分布式系统要维护（可用 Temporal Cloud 缓解）
- **学习曲线**：团队需 1-2 周掌握 Workflow + Activity 编程模型
- **短流程延迟**：单次 Activity 约 5-20ms 额外延迟（ANC 以小时/天级流程为主，不敏感）
- **调试复杂**：replay-based 模式要求 Workflow 函数确定性

**结论：对 ANC 净收益。** 工作流引擎不是 ANC 的核心差异化（核心是 Builder Agent + IR + 模块系统），不值得自研。Temporal 让 ANC 在 Phase 1 就获得企业级工作流能力。

### 15.3 其他技术栈决策

| 决策项 | 选择 | 理由 |
|---|---|---|
| **前端框架** | **React**（推荐 Next.js 或 Remix） | 生态丰富，SSR 支持好，招聘容易 |
| **移动端** | **H5（PWA）** | Phase 1 扫码操作端用 H5 即可，不需要原生 App |
| **数据库** | **PostgreSQL** | 关系型数据（业务对象、流程实例、日志）+ JSONB（IR 快照）兼顾 |
| **LLM 网关** | **LiteLLM** | 支持多模型 Provider 接入、成本统计、fallback，开源可自建 |
| **API 风格** | **gRPC（内部服务间）+ REST（对外 API）** | gRPC 服务间通信效率高，REST 兼容性更好 |

### 15.4 暂不做决策（保留评估）

| 项目 | 说明 | 评估时机 |
|---|---|---|
| **Container / K8s** | 开发阶段 Docker Compose 够用，生产环境具体方案待定 | Phase 1b 起 |
| **前端状态管理** | Zustand / Jotai / React Query，与 Next.js 集成后再定 | Phase 1a 中期 |
| **CI/CD 工具** | GitHub Actions / GitLab CI，与代码仓库绑定 | Phase 1a 初期 |
| **监控告警** | OpenTelemetry + Grafana / Datadog | Phase 1b 起 |
| **模型网关缓存** | Redis / Memcached，取决于 LLM 调用量 | Phase 1b 评估 |

---

## 16. 下一步工作

1. 设计资产盘点 / 仓储出入库 Demo 脚本；
2. 定义 Business IR v0.1；
3. 定义 Builder Agent 的需求澄清问题库；
4. 定义数据模型、页面、流程、权限的生成格式；
5. 定义 Business Module 和 AI Resource 的依赖锁定格式；
6. 定义 App Run Log、Workflow Run Log、Agent Run Log 的最小字段；
7. 实现第一个端到端 Demo；
8. 找 2-3 个数字化实施服务商访谈验证。
