# 腾讯 Marvis AI 调研报告

> 研究对象：腾讯 Marvis（马维斯）操作系统级 AI 助手  
> 研究时间：2026-05-30  
> 目的：理解 Marvis 的技术架构、核心能力、与 ANC（AI 原生企业业务操作系统）的差异和可借鉴之处。

---

## 1. 总体结论

Marvis 是腾讯于 2026 年 5 月 20 日正式发布的**操作系统级 AI 助手**，由腾讯应用宝团队开发。它不是普通聊天机器人，而是把整台电脑变成可对话对象的 AI Agent 系统。

Marvis 与 ANC 的核心差异在于：

| 维度 | Marvis | ANC |
|---|---|---|
| 定位 | 个人桌面 OS Agent | 企业业务系统生成平台 |
| 操作对象 | 现有文件、应用、系统设置 | 从零生成的业务应用、数据模型、流程 |
| 目标用户 | 个人知识工作者、消费者 | 企业数字化负责人、实施服务商 |
| 核心价值 | 操控现有系统、减少手动操作 | 构建和维护企业业务系统 |
| 产品形态 | 桌面客户端（Win/Mac/Android） | Web 平台 + Agent 工作空间 |

但 Marvis 在**多 Agent 协作架构、混合路由策略、隐私信任设计、长任务 UX** 等方面有值得 ANC 借鉴的技术决策。

---

## 2. 产品定位与目标用户

### 2.1 定位

腾讯对 Marvis 的定位：

> 不是聊天机器人，而是能够把整台电脑变成可对话对象的 AI 助手。

官网核心卖点：

- 本地大模型，本地模式文件 0 上传；
- 手机随时操控电脑；
- 文件智能整理搜索；
- 一句话完成电脑设置；
- 文件深度理解与生成。

### 2.2 目标用户

从官网场景来看，Marvis 主要面向个人用户：

- 追星搭子（搜索资讯、微博签到、物料整理）
- 游戏伴侣（福利监控、行动规划、自动每日任务）
- 情报监控（大厂情报、新闻推送、票务搜集）
- 知识管理（书籍蒸馏、笔记提炼、求职筹备）
- 打工助手（文件格式转换、合同审查、数据分析）
- 电脑管家（系统设置、清理、网络修复）
- 成长加速（英语学习、文献整理）
- 生活助手（电影推荐、宝宝相册、旅行攻略）

企业级场景（金融、HR）被提及但不是主线。

---

## 3. 核心技术架构

### 3.1 多 Agent 协作架构

Marvis 采用 **"1 个主管 Agent + 5 个专家 Agent"** 的协作架构：

| Agent | 职责 |
|---|---|
| 主管 Agent（Orchestrator） | 理解用户意图，分解任务，协调专家 Agent，汇总结果 |
| 文件 Agent（File Agent） | 文件搜索、归类、格式转换、内容理解、批量处理 |
| 电脑 Agent（Computer Agent） | 系统设置修改、硬件检测、网络修复、问题排查 |
| 应用 Agent（App Agent） | 调用和操作其他软件（Chrome、微信、Photoshop 等） |
| 浏览器 Agent（Browser Agent） | 浏览器内搜索、信息提取、网页操作 |
| 搜索 Agent（Search Agent） | 本地知识库语义搜索、网络信息检索 |

这种架构与 ANC 的 Agent 分工设计（Builder Agent + Schema/Workflow/UI/Test/Ops Agent）高度相似，都是一个 Orchestrator 协调多个 Specialist 的模式。

### 3.2 混合路由引擎（Hybrid Routing Engine）

Marvis 最核心的技术差异化是**任务复杂度评分 + 本地/云端混合路由**：

```text
用户指令
    ↓
任务复杂度评分（Task Complexity Scoring）
    ├── 轻量任务 → 本地端侧模型（Qwen 端侧版，离线运行，零 token 成本）
    └── 复杂任务 → 云端大模型（混元 Hunyuan3 / DeepSeek V4，端云协同）
```

关键设计决策：

- 不是所有请求都发往云端大模型，而是根据计算权重动态调度；
- 隐私模式下所有推理在本地完成，文件 0 上传，零 token 成本；
- 效率模式下端云协同，兼顾速度和能力；
- 2025 年 12 月锁定架构，不打算迁移到 Hermes 或 OpenClaw 等第三方框架。

**对 ANC 的启发**：ANC 的运行期已经确定"优先确定性代码，必要时才调用 LLM"。但构建期的 Agent 调用也可以借鉴混合路由思路——简单的模块匹配用规则引擎，复杂的业务建模才用 LLM，从而进一步降低构建成本。

### 3.3 OS 级系统接入

Marvis 通过腾讯与微软、英特尔、高通的硬件生态合作，获得了**操作系统底层权限**：

- 直接读写本地文件系统，无需用户手动上传；
- 修改系统设置（分辨率、电源计划、自启动项等）；
- 调用和操控其他桌面应用；
- 读取硬件配置信息；
- 屏幕录制和远程操控。

这种深度 OS 集成是**结构性壁垒**——没有等效的 OEM 合作关系，第三方云原生 Agent 产品无法复制。

**对 ANC 的启发**：ANC 的结构性壁垒不是 OS 接入，而是 Business IR + Module System + Company Context。定位不同，壁垒策略也不同。

### 3.4 端侧大模型与隐私模式

Marvis 提供两种运行模式：

| 模型 | 特点 | 适用场景 |
|---|---|---|
| 效率模式 | 端云协同，Hunyuan3 + DeepSeek V4 | 日常办公、内容生成、信息检索 |
| 隐私模式 | 全部本地推理，Qwen 端侧模型，断网可用，文件 0 上传 | 敏感数据、合同审查、企业金融/HR |

隐私模式的零 token 成本是企业级场景的重要卖点——随着 Agent 使用规模扩大，推理成本会成为关键约束。

**对 ANC 的启发**：ANC 的运行期已经设计为确定性执行为主（零 LLM 成本），但构建期的 LLM 调用成本控制仍然重要。可以考虑引入"轻量构建模式"——对已有模块的组合和参数调整用规则引擎完成，只在从零生成时才调用 LLM。

---

## 4. 核心能力详解

### 4.1 文件系统即知识库

Marvis 最具差异化的能力之一是把**本地文件系统当作预索引的、持续更新的知识库**：

- 按文档类型自动分类：课程作业、简历、合同、发票、图片库；
- 支持 OCR 内容识别、语义搜索、元数据分类；
- 不需要用户手动上传或建立索引，OS 级权限直接读取；
- 支持自然语言查询：`找一下我上个月写的关于 STM32 SPI 通信的技术文档`。

**对 ANC 的启发**：ANC 的 Company Context 概念类似——让 Agent 理解企业的组织结构、业务对象、流程、数据、文档。区别是 ANC 面向企业级数据（数据库、API、业务系统），而 Marvis 面向个人文件系统。

### 4.2 工作流编排

Marvis 支持用户用自然语言描述复杂重复性工作，创建自定义工作流：

```text
创建一个"发布 CSDN 文章"的工作流：
1. 打开我指定的 Markdown 文件
2. 检查格式和错别字
3. 生成标题、摘要和 5 个关键词
4. 自动打开 CSDN 创作中心
5. 帮我粘贴内容并设置文章属性
```

还支持定时任务：
- 每天早上 9 点，自动整理昨天的下载文件；
- 每周五下午 5 点，备份工作文档到 D 盘。

**对 ANC 的启发**：Marvis 的工作流是"描述目标，Agent 动态执行"的模式，类似 Floatboat 的 Workspace Agent 路线。ANC 应坚持"构建期 Agent 生成，运行期确定性执行"的差异化——企业级场景需要可审计、可回滚、可测试的确定性流程。

### 4.3 跨端远程协同

- 手机安装 Marvis APP，扫码连接电脑；
- 手机端实时查看电脑任务执行画面；
- 手机发送指令控制电脑完成任务；
- 支持远程查看屏幕和文件传输。

### 4.4 文档智能处理

Marvis 团队声称文档处理能力（格式转换、编辑、总结、分类）超过"市场上 95% 的产品"。同时**刻意不做 AI 辅助编码**——认为开发者工具市场已有强竞争者，选择聚焦知识工作者的文档处理需求。

---

## 5. UX 设计亮点

### 5.1 Agent 空闲状态动画

Marvis 为每个专家 Agent 设计了**动画办公场景空闲状态**（如 Agent 在工位上等待），用于减少用户在长任务执行期间的等待焦虑。

团队识别出的关键洞察：

> 等待时间焦虑是长周期 Agent 任务的主要用户流失触发点。

**对 ANC 的启发**：ANC 的 Builder Agent 生成应用可能需要较长时间（需求澄清 → 数据模型 → 页面 → 流程 → 权限 → 测试）。可以借鉴这种"过程可视化 + 状态反馈"设计，让用户看到每个 Agent 的工作进展，而不是面对一个空白等待界面。

### 5.2 任务进度面板

- 复杂任务显示实时进度条和步骤分解；
- 右侧任务面板可查看详细执行过程；
- 支持随时暂停、取消或修改任务；
- 任务完成后自动生成执行报告。

---

## 6. 对 ANC 的借鉴总结

### 6.1 值得借鉴的设计

| Marvis 设计 | ANC 可借鉴之处 |
|---|---|
| 1 Orchestrator + N Specialist Agent 架构 | ANC 已采用类似设计（Builder Agent + Schema/Workflow/UI/Test/Ops Agent），方向一致，继续坚持 |
| 任务复杂度评分 + 混合路由 | 构建期引入"规则引擎优先"策略：模块匹配和参数调整用规则，从零生成才用 LLM，降低构建成本 |
| 隐私模式 / 本地推理 | ANC 运行期已是确定性执行（零 LLM 成本），但可在构建期提供"离线构建模式"，基于已有模块库组合 |
| Agent 空闲状态动画 / 进度可视化 | Builder Agent 生成过程应有步骤级进度反馈，避免用户面对空白等待 |
| 文件系统即知识库 | Company Context 概念可参考，但 ANC 面向企业级数据（数据库、API、业务系统），不是个人文件 |
| 工作流从自然语言生成 | ANC 的 Builder Agent 已有此能力，但应坚持确定性编译输出，不是动态执行 |
| 文档智能处理聚焦（刻意不做编码） | ANC 也应有明确的能力边界——聚焦业务系统生成，不做通用编码助手、不做通用 RAG、不做通用 Bot |

### 6.2 不应照搬的设计

| Marvis 设计 | ANC 为何不应照搬 |
|---|---|
| OS 底层权限接入 | ANC 面向企业业务系统，不是桌面 OS 操控。壁垒应来自 Business IR + Module System，不是 OEM 合作 |
| 动态执行工作流 | 企业级场景需要确定性、可审计、可回滚的流程，不能依赖 Agent 临场判断 |
| 消费者场景覆盖（追星、游戏、生活） | ANC 聚焦企业数字化场景，不做个人生活助手 |
| 端侧模型依赖 | ANC 的运行期应以确定性代码为主，不依赖端侧模型推理能力 |

### 6.3 Marvis 验证了 ANC 的哪些判断

1. **多 Agent 协作是正确方向**：腾讯选择 1 Orchestrator + 5 Specialist 的架构，与 ANC 的 Agent 分工设计一致。
2. **成本控制需要架构级方案**：Marvis 用混合路由控制推理成本，ANC 用"构建期 AI + 运行期确定性"控制成本，思路一致。
3. **隐私和信任是企业级关键**：Marvis 专门设计隐私模式应对企业数据安全顾虑，ANC 的确定性运行时 + 审计日志也是同样的信任建设。
4. **UX 决定 Agent 产品成败**：Marvis 在 Agent 等待体验上投入设计，说明长任务 UX 是 Agent 产品的共性挑战。
5. **能力边界很重要**：Marvis 刻意不做编码，ANC 也应坚持不做通用 Bot / 通用 RAG / 通用自动化平台。

---

## 7. 与已有竞品分析的对比

proposal.md 第 2.2 节已分析了 Coze、n8n、Dify 三类产品。Marvis 代表了一个新品类——**OS 级个人 AI Agent**，与前三者都不相同：

| 产品 | 品类 | ANC 应学习什么 | ANC 不应变成什么 |
|---|---|---|---|
| Coze | Agent 应用开发平台 | AI 资源统一管理 | Bot Studio |
| n8n | 节点式自动化 | 确定性流程运行 | 画布式自动化工具 |
| Dify | LLM 应用工程化 | 模型网关、RAG、LLMOps | 问答/RAG 平台 |
| **Marvis** | **OS 级个人 AI Agent** | **混合路由成本控制、Agent 进度 UX、能力边界克制** | **桌面操控工具、个人助手、动态执行引擎** |

---

## 8. 参考来源

- 腾讯 Marvis 官网：https://marvis.qq.com/
- AIHub 报道：https://www.aihub.cn/news/tencent-marvis-official-launch/
- CSDN 使用教程：https://blog.csdn.net/weixin_44092861/article/details/161278597
- ChinaBiz Insider 深度分析：https://chinabizinsider.com/tencent-launches-marvis-beta-betting-os-level-ai-agent-can-reclaim-pc-screen-time-from-mobile/
- Edgen 报道：https://www.edgen.tech/news/post/tencents-new-ai-assistant-marvis-features-6-coordinated-agents
- Peerlist AI Agents News：https://peerlist.io/cnyouzige/articles/ai-agents-news--may-21-2026-tencent-launches-marvis-os-agent

---

## 9. 一句话总结

Marvis 验证了"多 Agent 协作 + 混合路由 + 隐私优先"是大厂选择的 AI Agent 架构方向，但它的战场是**个人桌面操控**，ANC 的战场是**企业业务系统生成与维护**。ANC 应借鉴其成本控制策略和 Agent UX 设计，但坚持"构建期 AI + 运行期确定性 + Business IR + Module System"的核心差异化路径。
